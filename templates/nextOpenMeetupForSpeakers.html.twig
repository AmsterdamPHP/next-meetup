{% extends 'base.html.twig' %}

{% block title %}List of next meetups{% endblock %}

{% block body %}
    <section class="section">
        <div class="container">
            <h1 class="title">
                Next meetup without speaker: {{ meetup.date | date('Y-m-d') }}
            </h1>
            <div class="box">
                <form method="post" action="{{ path('addSpeakerToMeetup', { date: meetup.date | date('Y-m-d')}) }}">
                    <div class="field">
                        <label class="label">Name</label>
                        <div class="control">
                            <input type="text" class="input" required="required" name="speaker-name">
                        </div>
                    </div>
                    <div class="field">
                        <label class="label">Contact information</label>
                        <div class="control has-icons-left">
                            <input type="email" class="input" required="required" name="speaker-contact">
                            <span class="icon is-small is-left">
                                <i class="fas fa-envelope"></i>
                            </span>
                        </div>
                    </div>

                    <div class="field">
                        <div class="control">
                            <button class="button is-link">Submit</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </section>
{% endblock %}
